module swappy::usdc_coin;

/// MVP placeholder USDC type for local/testing.
/// The canonical production USDC type address/module is not specified yet.
///
/// Note: module name intentionally avoids the OTW rule (uppercase(module) == struct name).
public struct USDC has store, drop {}


